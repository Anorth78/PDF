--- PAGE 1 ---
July 2020
Abhishek Pal (abhispal@cisco.com)
SeetaRama Raju Kalidindi(sekalidi@cisco.com)
Cisco Meeting Server (CMS)

--- PAGE 2 ---
§Recording
§Streaming
§Cisco Meeting Management (CMM)
Agenda

--- PAGE 3 ---
Recording

--- PAGE 4 ---
What is a recorder?
• CMS Recorder enables the capability to record meetings on CoSpaces.
• The transcoding takes place real-time on the Recorder Server, while writing a temp file to the 
NFS
• At the end of the call, the file is automatically converted into an MP4 format and stored on NFS.

--- PAGE 5 ---
Supported Deployments

--- PAGE 6 ---
•Recommended to run recorder on dedicated server.
•4 Physical Cores and 4GB RAM
•1 Core will support 2 Simultaneous recordings, using 0.5 GB of Memory per recording.
•Can support 8 simultaneous recordings.
Recorder Server Requirements:
Component Purpose vCPU RAM Minimum CPU Maximum CPU
Recorder 2 recordings 1 1 GB (0.5 GB per) 4 24

--- PAGE 7 ---
Recording
Step 1. Configure an NFS Share Folder on a Windows Server
Step 2. Configure and enable recorder on the Recorder server
1. Configure the Recorder to listen on the interface:
recorder listen a:8443
If the recorder is on the local CB, the interface must be the set to “loopback”:
recorder listen lo:8443
.. Cont’d

--- PAGE 8 ---
2. Set the certificate file to be used by the recorder:
recorder certs <key file> <certificate file>
3. Add the Call bridge certificates bundle to the Recorder trust store:
recorder trust <crt-bundle>
4. Specify the hostname/IP address of the NFS, and the directory on the NFS to Store the 
recordings:
recorder nfs<hostname/IP>:<directory>
5. Now enable the Recorder:
recorder enable

--- PAGE 9 ---
Step 3. Add the Recorder onto the CB using the API
1.Send an empty POST with https://<Callbridge_IP>:445/api/v1/recorders
2.Then, send a GET with the same URL in (a), copy the recorder ID
3.Set the URL of the recorder by sending a PUT with 
https://<Callbridge_IP>:445/api/v1/recorders/<recorderid> and add this in BODY before you 
execute the PUT :
url=https://127.0.0.1:8443(if the recorder is on the local CB)
or
url=https://<IP Address of recorder>:8443 (if the recorder is not on the local CB)

--- PAGE 10 ---
In case of CMS 2.9 or above,
you can do that from Configuration > API > /api/vi/recorders

--- PAGE 11 ---
Step 4. Join meeting using CMA App and use “Record” button to start recording.

--- PAGE 12 ---
Streaming

--- PAGE 13 ---
What is a Streamer?
• The Streamer enables the capability of meetings in Co-Spaces to be streamed.
• The Streamer sends the media to an external streaming server based on URI configured on 
CoSpace.
• RTMP is the supported protocol used by the streamer.

--- PAGE 14 ---
Supported Deployments

--- PAGE 15 ---
•Recommended to run on dedicated or combined with recorder
•4 Physical Cores and 4GB RAM
•1 Core will support 6 Simultaneous streams.
Streamer Server Requirements:
Component Purpose vCPU RAM Minimum CPU Maximum CPU
Streamer 6 concurrent 
streams
1 1 GB 4 32

--- PAGE 16 ---
Streaming
streamer listen a:8443
Step 2. Set the certificate file to be used by the streamer:
streamer certs streamer.keystreamer.crt
Step 3. Add the Call bridge certificates bundle to the streamer trust store:
streamer trust callbridge.crt
Step 4. Now enable the streamer: 
streamer enable
Step 1. Configure interface(s) for the streamer to listen:

--- PAGE 17 ---
1.Send a “POST” to https://<Callbridge_IP>:445/api/v1/streamers/ with below
url=https://<IP Address of streamer >:<port>
Step 6. Add the streamer to a CoSpace.
1.Send “GET” https://<CMS IP>:<port>/api/v1/Cospacesto get list of CoSpaceyou have created 
along with their ID’s.Here copy the CoSpaceID to which you want to assign this streamer to.
.. Cont’d
Step 5. Add the streamer onto the CB using the API

--- PAGE 18 ---
In case of CMS 2.9 or above,
you can do that from Configuration > API > /api/vi/streamers

--- PAGE 19 ---
2. Sent “PUT”https://<CMS IP>:<Port>/api/v1/Cospaces/<CoSpace ID> with below
streamURL= rtmp://<VBrickBroadcastUsername>:<VBrickBroadcastPassword>@<VBrickIP or 
FQDN>/live/NameoftheStream
Step 7: Join the meeting from CMA, Click on “Streaming” to start streaming and verify stream on 
using stream URL.

--- PAGE 20 ---
Cisco Meeting Management

--- PAGE 21 ---
•Cisco Meeting Managementis a management tool forCisco's on-premises video meeting 
platform,Cisco Meeting Server. 
•You can use the tool to monitor and manage meetings that are running on the platform, and it 
also provides information about whichCiscolicenses you are using.
What is CMM?

--- PAGE 22 ---
•Meeting Management can manage anything from a single Call Bridge to multiple clustered deployments. 
•The VM requirements depend on your deployment size. See the capacity table below to determine your deployment 
size.
VM-Requirements

--- PAGE 23 ---
•The VM is configured for small to medium deployments. For large deployments, you must change the sizing manually 
during setup.
•If you have a medium size deployment and think you may need higher capacity later, then configure your VM for a 
large deployment
VM-Requirements

--- PAGE 24 ---
•Single instance of CMM can manage 
deployments from single CallBridgeto 
Cluster of CallBridge
•NTP servers synchronize Meeting 
Servers and Meeting Management 
instances
•External syslog server for system and 
audit logs
•User access is authenticated via LDAP
CMM-Single Deployment

--- PAGE 25 ---
•Up to 2 instances of CMM supported for 
added resiliency 
•CMM instances configured 
independently (active: active)
•Recommended to deploy the 2 instances 
in different locations 
•No information is exchanged between 
the two CMM
•Create a certificate for each instance of 
CMM
•Each certificate must include both the 
user facing address and the unique CDR 
receiver address
CMM-Resiliency Deployment

--- PAGE 26 ---
•Meeting Management uses a certificateto identify itself to browsers and to Call Bridges.
•Setup, Meeting Management generates a self-signed certificate which you can use during initial configuration.
•In a production environment, you must replace the self-signed certificate with a certificate signed by a CA
(Certificate Authority). You can use an internal or external CA, depending on the requirements in your organization.
•The certificate chain should include the certificate of the CAthat signed the certificate, plus any certificates higher in 
the certificate chain, up to and including the root CA certificate.
•Your CDR receiver address, as well as any addresses your users will use for the browser interface, should be 
included in the certificate.
•When the SANfield is used, Meeting Management does not look at the Common Name. The CDR receiver address 
must be included in the SANfield.
•Meeting Management has no capability to create certificate signing requests. Use a dedicated tool, for instance the 
OpenSSL toolkit, to create your private key and a certificate signing request. CMS server can also be used.
Certificate Requirement-CMM

--- PAGE 27 ---
•Meeting Management supports locally managed users as well as user authentication via LDAP
•Local users are added and managed locally on the Meeting Management Users page. These users are authenticated 
directly by Meeting Management.
•LDAPusers are added via mappings to existing groups on your LDAPserver. Meeting Management uses your 
LDAPserver to authenticate these users by checking their group membership when they sign in
•Authentication via LDAP is recommended for general use and administration
Users can have two roles: 
Administratorshave full access to Meeting Management. Administrators will typically set up Meeting Management, 
change configurations, add users, and monitor and maintain the system.
Video operators only have access to the Meetings and Overview pages. Video operators monitor and manage 
meetings, and they perform basic troubleshooting related to ongoing meetings. For instance, they may try to call a 
participant who got disconnected or check the call statistics if someone has audio issues.
CMM-LDAP & Users

--- PAGE 28 ---
•See list of active or recent meetings 
•Search by meeting title or individual 
participant 
•Pin meeting at top of the list
•See connected Cisco Meeting Servers 
•Add and remove Call Bridge nodes or 
clusters
CMM-Feature Overview

--- PAGE 29 ---
•View connection details per participant 
•Mute audio/video individually
•Drop active participant from meeting
•Per participant layout change/override 
conference wide setting
•Detailed audio and video call 
information.
•Move participants in CMM from one 
space to other.
CMM-Feature Overview -Participant Detail

--- PAGE 30 ---
•List overview of all participants in a 
meeting 
•Conference wide layout change per 
meeting
•Drop active participant from meeting
•Start and stop recording and streaming * 
•Controls to mute audio/video and drop 
participants
•View and download event log for a 
meeting
CMM-Feature Overview -Meeting Detail

--- PAGE 31 ---
•List all user licenses Including recorder 
and streamer
•List licenses utilization
•Shows graphical utilization of licenses
•List the licenses events
CMM-CMS Licenses

--- PAGE 32 ---
Questions?